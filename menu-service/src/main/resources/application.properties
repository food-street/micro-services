spring.application.name=menu-service
spring.profiles.active=${SPRING_PROFILE:local}

# Database
spring.data.mongodb.uri=${USER_DB_URI:mongodb://root:password@localhost:27018/menu_db?authSource=admin}

# Enable DEBUG logging for Spring's web package
logging.level.org.springframework.web=DEBUG

# Restaurant service URL
restaurant.service.url=${RESTAURANT_SERVICE_URL:http://localhost:8084}